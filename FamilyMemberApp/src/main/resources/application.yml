server:
  port: 8081

spring:
  application:
    name: FamilyMemberApp
  datasource:
    username: "sebastian"
    password: "password"
    url: jdbc:postgresql://postgres:5432/familymemberdb
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true