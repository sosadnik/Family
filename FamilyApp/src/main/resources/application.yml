server:
  port: 8080

spring:
  application:
    name: FamilyApp
  datasource:
    username: "postgres"
    url: jdbc:postgresql://127.0.0.1:5432/FamilyDB
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true