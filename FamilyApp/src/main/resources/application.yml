server:
  port: 8080

spring:
  application:
    name: FamilyApp
  datasource:
    username: "sebastian"
    password: "password"
    url: jdbc:postgresql://postgres:5432/familydb
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true